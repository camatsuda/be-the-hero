# Test dependencies for WebApp Analytics ETL Pipeline
# Note: unittest.mock is part of Python standard library since 3.3, no separate package needed

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Code quality
pylint>=2.17.0
black>=23.7.0
mypy>=1.4.1

# For running tests with different Python versions (optional)
tox>=4.6.4
